README

	GameStats is a JavaGui program that allows table creation into an hml
	file for organization and simpler input for fighting games. Once
	downloaded, you may run the file executable "SupStats" that will